# Outline Generator v1

Generate a concise, export-safe outline. Do not write full paragraphs. Use section or slide titles, content intent, visual intent, and citation needs.

Input intent JSON:

```json
{{intent_json}}
```

Return JSON only.

